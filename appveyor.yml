# appveyor file
# http://www.appveyor.com/docs/appveyor-yml
environment:
  matrix:
  # node.js
    - nodejs_version: "6"

platform:
  - x86
  - x64

build: off

# scripts that run after cloning repository
install:
  - npm install
  - npm run gulp build
  # - npm run dist-win
